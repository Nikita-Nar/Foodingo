import React from 'react';
import {Text, View} from 'react-native';

const ExpertLevel = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>This is Expert Level</Text>
    </View>
  );
};
export default ExpertLevel;